# Production dependencies for Vercel deployment.
# Full dev/research deps (pymc, jupyter, lifelines, etc.) live in backend/requirements.txt.
#
# IMPORTANT: Only add packages that are actually imported by backend/ code.
# Unused deps bloat the Lambda bundle (500MB limit). Verify with:
#   grep -r "import <pkg>" backend/

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
httpx>=0.27.0

# Core simulation (Monte Carlo engine)
numpy>=1.26.0
scipy>=1.13.0

# Bayesian belief network inference (bbn_lite replaces pgmpy to avoid
# pgmpy's 9.7GB torch dependency — see backend/services/bbn_lite.py)
# Data manipulation (pandas used by data_loader, spatial, equity)
pandas>=2.2.0
