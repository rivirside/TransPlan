# Production dependencies for Vercel deployment.
# Full dev/research deps (pymc, jupyter, lifelines, etc.) live in backend/requirements.txt.
#
# IMPORTANT: Only add packages that are actually imported by backend/ code.
# Unused deps bloat the Lambda bundle (250MB limit). Verify with:
#   grep -r "import <pkg>" backend/services/ backend/routers/

fastapi>=0.110.0
pydantic>=2.6.0

# Core simulation (Monte Carlo + spatial interpolation)
numpy>=2.0.0
scipy>=1.13.0

# NOT needed on Vercel (verified with grep):
# uvicorn — Vercel provides its own ASGI runtime
# httpx — not imported by any backend service
# pandas — only used in notebooks/scripts
# lifelines, openpyxl, xlrd — data pipeline only
# pgmpy — replaced by backend/services/bbn_lite.py
